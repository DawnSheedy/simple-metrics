apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sm-service.fullname" . }}-test-connection"
  labels:
    {{- include "sm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
